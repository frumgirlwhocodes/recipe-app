
<h2> Our Library of Recipes</h2>
<br> 


<% @recipes.each do |recipe| %>
    <h2><%= link_to recipe.name, user_recipe_path(recipe.id)%></h2>
    <% end %>

    <h3>  Recently Added Recipes </h3> 
<% @recent_recipes.each do |recipe| %>
   <h3> <%= recipe.name %> </h3> 
   <%= link_to recipe.name, user_recipe_path(recipe, recipe.user_id) %> 
   <% end %> 


<h3> Add a New Recipe </h3> 

<%= link_to "Add A New Recipe", new_user_recipe_path %>