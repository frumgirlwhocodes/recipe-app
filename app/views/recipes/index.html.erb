
<h2> Our Library of Recipes</h2>
<br> 


<% @recipes.each do |recipe| %>
    <h2><%= link_to recipe.name, recipe_path(recipe)%></h2>
    <% end %>
<br>
    <h3>  Recently Added Recipes </h3> 
<% @recent_recipes.each do |recipe| %>
   <h3> <%= recipe.name %> </h3> 
   <br> 
   <%= link_to "click to view", recipe_path(recipe) %> 
   <% end %> 


<h3> Add a New Recipe </h3> 

<%= link_to "Add  Recipe", new_recipe_path %>